// Task 4 (Hard)
// Reverse the words of a sentence. Only the position of the word will be reversed. check out the output

// Input:

const statement = "I am a hard working person";
const output = statement.split(" ").reverse().join(" ");
// split er vitor white space diye sentence ta k white space er maddhome
// prottek ta word split korse..diye split kora ongsho k reverse korse diye
//  reverse kora ongsho k join er vitor white space diye word gula k add korse

console.log(output);

// Output:

// 'person working hard a am I'
